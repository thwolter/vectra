services:
  otel-collector:
    image: otel/opentelemetry-collector:latest
    command: ["--config=/etc/otelcol/config.yaml"]
    volumes:
      - ./docker/otel-collector-config.yaml:/etc/otelcol/config.yaml:ro
    healthcheck:
      test: [ "CMD", "/otelcol", "validate", "--config=/etc/otelcol/config.yaml" ]
      interval: 10s
      timeout: 5s
      retries: 3
    ports:
      - "4318:4318"

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command:
      - redis-server
      - --requirepass
      - redispassword
    ports:
      - '6379:6379'
    healthcheck:
      test: ['CMD', 'redis-cli', '-a', 'redispassword', 'ping']
      interval: 10s
      timeout: 3s
      retries: 5
    volumes:
      - redis-data:/data

  vectra-db:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    environment:
      POSTGRES_DB: vectra
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d vectra']
      interval: 10s
      timeout: 5s
      retries: 1
    ports:
      - '5432:5432'
    volumes:
      - ./docker/init:/docker-entrypoint-initdb.d:ro
      - vectra-db-data:/var/lib/postgresql/data


  vectra:
    build:
      context: ./
    depends_on:
      redis:
        condition: service_healthy
      vectra-db:
        condition: service_healthy
      otel-collector:
        condition: service_healthy
    environment:
      POSTGRES_URL: postgresql://${VECTRA_USER:-vectra_user}:${VECTRA_PASSWORD:-vectra-password}@vectra-db:5432/vectra
      # ALEMBIC_DATABASE_URL: postgresql://${ALEMBIC_USER:-alembic_user}:${ALEMBIC_PASSWORD:-alembic-password}@vectra-db:5432/vectra
      # We must use the super admin user to create the alembic tables - todo: to be fixed later
      ALEMBIC_DATABASE_URL: postgresql://postgres:postgres@vectra-db:5432/vectra
      REDIS_URL: redis://:redispassword@redis:6379/0
      DRAMATIQ_BROKER_URL: redis://:redispassword@redis:6379/0
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
      AWS_S3_BUCKET: ${AWS_S3_BUCKET}
      AWS_REGION: ${AWS_REGION}
      DOCUMENT_STORE: local
      LOCAL_FILE_PATH: /tmp/vectra/documents
      JWT_SECRET: ${JWT_SECRET}
      PORT: 8000
      START_WEB: 'true'
      START_WORKER: 'true'
      DEPLOY_SKIP_MIGRATIONS: 'false'
      LOG_LEVEL: INFO
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://otel-collector:4318}
      OTEL_TRACES_EXPORTER: ${OTEL_TRACES_EXPORTER:-otlp}
      OTEL_METRICS_EXPORTER: ${OTEL_METRICS_EXPORTER:-otlp}
      OTEL_LOGS_EXPORTER: ${OTEL_LOGS_EXPORTER:-otlp}
    ports:
      - '8002:8000'
    volumes:
      - /tmp/vectra/documents:/tmp/vectra/documents
    develop:
      watch:
        - action: sync
          path: ./app
          target: /app/app
        - action: sync
          path: ./scripts
          target: /app/scripts
        - action: sync
          path: ./alembic
          target: /app/alembic
        - action: sync
          path: ./alembic.ini
          target: /app/alembic.ini
        - action: rebuild
          path: ./pyproject.toml
        - action: rebuild
          path: ./uv.lock

volumes:
  redis-data:
  vectra-db-data:
